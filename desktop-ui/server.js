// Compiled/transpiled version of server.ts for Electron
// This file can be generated from server.ts using tsx or tsc
// For now, we'll use tsx to run it directly

require('tsx/cjs/api').register({
  transpileOnly: true
});

require('./server.ts');
